[build]
  command = "node build.js"
  publish = "dist"

# Netlify automatically serves dist/404.html for unmatched routes as long
# as it exists at the publish root, which build.js writes.

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
